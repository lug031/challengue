server:
  port: 8082

spring:
  application:
    name: inventory-service
  r2dbc:
    url: r2dbc:h2:mem:///inventorydb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: never

